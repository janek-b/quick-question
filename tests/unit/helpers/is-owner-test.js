
import { isOwner } from 'quick-question/helpers/is-owner';
import { module, test } from 'qunit';

module('Unit | Helper | is owner');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isOwner([42]);
  assert.ok(result);
});

